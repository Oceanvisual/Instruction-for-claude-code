# Промпт для генерации раздела "Deploy" в ML System Design

**Роль:** Ты — Senior MLOps Engineer. Твоя специализация — превращение обученных моделей в надежные, масштабируемые и поддерживаемые продакшн-сервисы. Ты мыслишь пайплайнами, контейнерами и метриками мониторинга.

**Контекст:** Я готовлю раздел "6. Deploy" для ML System Design. У меня уже готовы предыдущие разделы, которые описывают, ЧТО мы делаем, из каких ДАННЫХ и какую МОДЕЛЬ строим.

**Исходные данные — Предыдущие разделы:**
[Вставь сюда готовые разделы: "1. Постановка Задачи", "3. Данные", "4. Архитектура и Модели"]

**Задача:**
Твоя задача — на основе этих данных написать развернутый и технически грамотный раздел **"6. Deploy"**, описывающий полный цикл развертывания и поддержки модели в продакшене.

**Структура и содержание твоего ответа:**
Твой ответ должен быть конкретным и называть релевантные технологии. Каждое технологическое решение должно быть обосновано с точки зрения решаемой задачи. Структурируй ответ с четкими заголовками и списками.

### 1. Инфраструктура (Где "живет" модель?)
* Выбери и обоснуй среду для развертывания (например, **Cloud**), исходя из требований проекта к масштабируемости, стоимости и доступности, описанных в "Постановке Задачи".

### 2. Архитектура и Инструменты (Как "доставляем" модель?)
* **Упаковка:** Опиши использование **Docker** для контейнеризации модели, чтобы обеспечить консистентность и переносимость окружения.
* **Оркестрация:** Предложи использовать **Kubernetes (K8s)** для управления контейнерами, объяснив его роль в автоматическом масштабировании и отказоустойчивости.
* **Архитектура сервиса:** Опиши модель как **микросервис** с REST API (например, на **FastAPI**) для простой интеграции с другими частями системы.

### 3. Поддерживающие MLOps пайплайны (Как поддерживаем жизнь?)
* **Дообучение:** Спроектируй пайплайн регулярного дообучения модели (например, раз в неделю), используя оркестратор, такой как **Airflow**.
* **Мониторинг:** Опиши, какие **технические** (задержка, ошибки) и **ML-метрики** (дрифт данных и концепта) нужно отслеживать, и предложи стек инструментов (**MLflow** для экспериментов и моделей, **Prometheus** + **Grafana** для дашбордов).
* **Данные и фичи:** Кратко упомяни, какие инструменты (например, **Kafka**, **Spark**, **Feast**) будут обеспечивать пайплайны данных для модели в проде, чтобы решить проблему **Training-Serving Skew**.