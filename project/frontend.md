# 🤖 Промпт для Создания и Аудита Streamlit-Фронтенда

**Роль:** Ты — мой ассистент, Senior Streamlit Developer.

**Задача:** Создать универсальный, интуитивно понятный и функциональный веб-интерфейс (front-end) для готовой модели машинного обучения, а затем провести критический анализ своей работы.

---

### 📝 Контекст и Основная Идея

Проанализируй **контекст текущей ML-задачи** (тип модели, какие данные ей нужны на вход и что она возвращает на выход). На основе этого анализа создай полноценное Streamlit-приложение.

Приложение должно быть построено по принципу "сначала главное", быть удобным для пользователя и следовать лучшим практикам Streamlit.

---

### Часть 1: Структура Приложения

Создай многостраничное приложение со следующей структурой:

#### 1. Главная страница: `Прогноз`

Это основной экран для взаимодействия с моделью.

* **Сайдбар для ввода данных (`st.sidebar`):**
    * Создай в сайдбаре форму для пользовательского ввода.
    * Для **каждого признака, необходимого модели**, подбери наиболее подходящий виджет (`st.slider`, `st.selectbox`, `st.number_input`, `st.text_input` и т.д.). *Твой выбор виджетов должен основываться на типе данных (число, категория, текст) каждого признака из контекста задачи.*
    * Добавь кнопку (`st.button`), по нажатию на которую будет запускаться модель и выводиться прогноз.

* **Основная область для вывода результата:**
    * Крупно и наглядно покажи **результат прогноза** (например, через `st.metric`, `st.success`/`st.error` или просто крупный заголовок).
    * Под результатом добавь `st.expander` с названием "Детали прогноза", в котором будет отображаться дополнительная информация (например, вероятность классов, SHAP-график или другие метрики, которые возвращает модель).

---

### Часть 2: Финальные Требования к Коду

* **Чистый код:** Структурируй код, вынося логику и отдельные блоки UI в Python-функции.
* **Производительность:** Используй кэширование (`@st.cache_data`) для загрузки данных или других ресурсоёмких операций.
* **Лучшие практики:** Следуй гайдлайнам для разработки на Streamlit (например, используй `st.session_state` при необходимости хранить состояние между действиями пользователя).

---

### Часть 3: Саморефлексия и Критика

После того как ты сгенерируешь код, проведи его строгий и честный критический анализ.

1.  **Проверка на ошибки:** Пересмотри свой код. Нет ли в нём потенциальных багов, логических противоречий или неэффективных участков, которые могут замедлить работу приложения?
2.  **Следование гайдлайнам:** Убедись, что ты следовал всем указанным лучшим практикам для Streamlit (использование `st.session_state`, кэширование, уникальные ключи для виджетов и т.д.). Если где-то отошёл от них, объясни почему.
3.  **Оценка User Experience (UX):** Подумай с точки зрения конечного пользователя. Насколько приложение интуитивно и понятно? Можно ли что-то улучшить в расположении элементов или в подаче информации, чтобы сделать его более удобным?
4.  **Итоговое улучшение:** Предложи **одно ключевое улучшение** для этого приложения, которое ты бы реализовал на следующей итерации (например, обработка крайних случаев, добавление нового функционала, улучшение визуализации). Обоснуй свой выбор.