@data-preprocessing-rule.mdc 

# 🤖 Промпт для Глубокого Аудита ML-кода

**Роль:** Ты — мой ассистент, который теперь выступает в роли дотошного и опытного ревьюера собственного кода (Lead ML Code Reviewer).

**Задача:** Провести глубокий критический анализ сгенерированного тобой пайплайна (от предобработки до валидации). Твоя цель — найти не только очевидные ошибки, но и скрытые методологические проблемы.

---

###  Часть 1: Целостность Валидации и Утечка Данных (Validation Integrity & Data Leakage)

Это самый важный блок. Проверь код на все возможные виды утечек данных.

1.  **Порядок операций:** В какой последовательности выполнялись шаги: разделение данных на train/test и предобработка (imputation, scaling, encoding)?
2.  **Обучение препроцессоров:** На каких конкретно данных (train, test или на всех вместе) вызывался метод `.fit()` для каждого шага предобработки (например, `SimpleImputer`, `StandardScaler`)?
3.  **Проверка внутри кросс-валидации:** Если используется кросс-валидация, убедись, что все шаги предобработки (`fit_transform`) происходят **внутри** каждого фолда на обучающей части, а на валидационной — только `transform`. Это касается и техник балансировки, таких как SMOTE.
4.  **Проверка временных данных:** Есть ли в данных временная компонента (даты, порядок событий)? Если да, соответствует ли стратегия валидации этому, чтобы избежать "заглядывания в будущее" (например, используется `TimeSeriesSplit` вместо обычного `KFold`)?
5.  **Утечка цели (Target Leakage):** Проанализируй признаки. Нет ли среди них тех, которые могли быть созданы с использованием информации о целевой переменной и не были бы доступны в момент реального прогноза?

---

###  Часть 2: Глубокий Анализ Предобработки (In-Depth Preprocessing Analysis)

Детально обоснуй принятые тобой решения по каждому шагу.

1.  **Стратегия работы с пропусками:** Почему ты выбрал именно этот метод импутации (например, медиана, а не среднее)? Учитывал ли ты распределение данных при этом выборе (например, наличие выбросов)?
2.  **Логика обработки выбросов:** Как именно ты обрабатывал аномалии и выбросы (удаление, ограничение)? Как ты определил порог для того, что считать выбросом? Оценил ли ты риск потери важной информации при удалении этих данных?
3.  **Обоснование кодирования:** Почему для категориальных признаков был выбран именно этот метод кодирования? Рассматривал ли ты альтернативы для признаков с большим количеством уникальных значений (high-cardinality features)?
4.  **Необходимость масштабирования:** Обязательно ли было масштабировать данные для выбранного типа модели? Объясни свой выбор.

---

###  Часть 3: Стратегия и Критическое Мышление (Strategy & Critical Thinking)

Теперь подумай над общей архитектурой решения и предложи улучшения.

1.  **Самокритика:** Посмотри на свой код свежим взглядом. Какие в нём есть потенциальные слабости или "узкие места"? Существуют ли более простые, надёжные или эффективные способы решить те же задачи?
2.  **Ключевое улучшение:** Предложи **одно главное улучшение** для всего пайплайна, которое могло бы значительно повысить его надёжность или качество модели. Это может быть изменение в предобработке, стратегии валидации или выборе модели. Обоснуй свою идею.