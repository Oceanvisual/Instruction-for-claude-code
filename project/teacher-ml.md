# 🤖 Промпт для Обучения и Сравнения ML Моделей

**Задача:** Систематически обучить, сравнить и проанализировать несколько моделей машинного обучения для решения поставленной задачи, выбрав лучшую и определив дальнейшие шаги по её улучшению.

---

### 📝 Контекст Задачи

* **Данные:** `X_train`, `X_val`, `y_train`, `y_val` *(или `X`, `y` для кросс-валидации)*
* **Тип задачи:** *(например, бинарная классификация, регрессия)*
* **Целевая метрика:** *(например, `ROC AUC`, `F1-score`, `MAE`)*

---

### Часть 1: Стратегия и Бейзлайн

Прежде чем обучать сложные модели, необходимо заложить фундамент и определить точку отсчёта.

1.  **Определи и Обоснуй Бейзлайн:**
    * **Что это?** Бейзлайн — это простая, быстрая и интерпретируемая модель (например, `LogisticRegression` для классификации или `LinearRegression` для регрессии).
    * **Зачем он нужен?** Он служит **точкой отсчёта**. Любая более сложная модель должна доказать, что она работает значительно лучше этого простого решения. Если нет — усложнение не имеет смысла.
    * **Твоя задача:** Выбери и кратко обучи бейзлайн-модель.

---

### Часть 2: Предмодельный Чек-лист

Перед обучением основного пула моделей, продумай и явно опиши свою стратегию по следующим критически важным пунктам.

1.  **Стратегия Кросс-Валидации:**
    Проанализируй данные и целевую переменную. Выбери и **обоснуй** наиболее подходящую стратегию: `StratifiedKFold` (для задач классификации с дисбалансом), `TimeSeriesSplit` (для временных рядов, где порядок важен) или стандартный `KFold`.

2.  **Проблема Дисбаланса Классов:**
    Проверь баланс классов в `y_train`. Если он сильно нарушен, предложи и реализуй стратегию борьбы с этим (например, использование параметра `class_weight='balanced'` в моделях, `SMOTE` или выбор F1-score как основной метрики).

3.  **Масштабирование Признаков (Scaling):**
    Оцени, нуждаются ли выбранные тобой модели в масштабировании данных (например, линейные модели, SVM). Если да, обязательно включи `StandardScaler` или `MinMaxScaler` в пайплайн, чтобы избежать утечки данных.

4.  **Воспроизводимость Результатов:**
    Установи единый `random_state` (или `seed`) для всех процессов (разделение данных, обучение моделей), чтобы обеспечить полную воспроизводимость твоих результатов.

---

### Часть 3: Обучение и Оценка Основных Моделей

Теперь перейдём к сравнению более мощных моделей с нашим бейзлайном.

1.  **Перебор Моделей:**
    Выбери 2-3 или более подходящих для этой задачи и более сложные модели (например, `LightGBM`, `CatBoost`, `RandomForest`, `XGBoost`).

2.  **Расчёт и Сравнение Метрик:**
    Для **каждой** модели (включая бейзлайн) посчитай на кросс-валидации или валидационной выборке:
    * **Целевую метрику.**
    * **Дополнительные релевантные метрики** (например, `Precision`, `Recall`, `Accuracy` для классификации; `RMSE`, `R²` для регрессии).
    * Представь результаты в виде **сводной таблицы** для наглядного сравнения.

3.  **Анализ Важности Признаков (Feature Importance):**
    * Для лучшей модели (или для всех древовидных моделей) извлеки и визуализируй **топ самых важных признаков**.
    * Сделай **краткий, но осмысленный вывод:** какие факторы вносят наибольший вклад в предсказание? Соответствует ли это бизнес-логике?

---

### Часть 4: Анализ, Самокритика и Следующие Шаги

Финальный и самый важный этап — осмысление результатов и планирование будущего.

1.  **Сводный Анализ:**
    * Какая модель показала себя лучше всего по целевой метрике?
    * Насколько сильно она превзошла бейзлайн? Является ли это улучшение значимым?

2.  **Самокритика:**
    * Какие потенциальные слабости были в твоём подходе?
    * Мог ли какой-то из шагов в чек-листе (Часть 2) быть выполнен иначе и лучше?
    * Достаточно ли было выбранных моделей для объективного сравнения?

3.  **Креативные Следующие Шаги:**
    Основываясь на результатах, предложи **один главный шаг** для дальнейшего улучшения лучшей модели. Выбери и обоснуй одно из направлений:
    * **Тюнинг Гиперпараметров:** Предложи стратегию подбора оптимальных гиперпараметров для лучшей модели (например, `Optuna` или `GridSearchCV`), указав ключевые параметры для тюнинга.
    * **Feature Engineering:** Основываясь на анализе `feature importance`, предложи идею для создания нового, потенциально сильного признака.
    * **Анализ Ошибок (Error Analysis):** Предложи, как можно проанализировать объекты, на которых лучшая модель ошибается сильнее всего, чтобы найти скрытые закономерности.


### Часть 5: Сохранение Лучшей Модели

После определения лучшей модели её необходимо сохранить в виде файла для дальнейшего использования (например, для деплоя или инференса).

1.  **Сохрани Артефакт:**
    * Используй библиотеку `joblib` для сохранения **обученного объекта** лучшей модели.
    * Присвой файлу осмысленное имя, включающее название модели и её скор, например: `best_lgbm_model_roc_auc_0.85.joblib`.