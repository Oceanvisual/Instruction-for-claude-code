# Промпт для Создания README.md из Kaggle Соревнования

**Задача:** Проанализировать страницу Kaggle соревнования по предоставленной ссылке и на её основе создать исчерпывающий `README.md` файл для репозитория проекта.

**Ключевая цель:** Этот `README.md` должен служить "мозгом" проекта. Он должен быть настолько качественным, чтобы в будущем ты, AI-ассистент, мог прочитать его и мгновенно понять весь контекст задачи, данных и целей для выполнения дальнейших запросов.

---

### 📥 Входные данные

* **Описание соревнования:** `[Вставь сюда описания соревнования с Kaggle]`

---

### Требуемая Структура и Содержание README.md

Ты должен сгенерировать `README.md`, который будет иметь следующую структуру. Для каждого раздела используй информацию со страницы соревнования.

#### ## 1. Заголовок и Источник
* **Действие:** Извлеки полное название соревнования и используй его как основной заголовок (`#`).
* **Действие:** Сразу под заголовком добавь прямую ссылку на соревнование для быстрой навигации.

#### ## 2. Описание Задачи (Problem Statement)
* **Действие:** Внимательно изучи разделы "Description" и "Overview" на Kaggle.
* **Результат:** Своими словами, но близко к тексту, опиши **бизнес-проблему**. Какую задачу мы решаем? Почему это важно? Укажи тип задачи (например, бинарная классификация, регрессия, детекция объектов).

#### ## 3. Данные (Data)
* **Действие:** Перейди в раздел "Data" на странице соревнования.
* **Результат:**
    * Опиши предоставленные файлы (`train.csv`, `test.csv`, `sample_submission.csv` и т.д.).
    * Кратко опиши, что означают данные (контекст признаков).
    * Самое главное: чётко определи, какой столбец является **целевой переменной (target)**.

#### ## 4. Метрика Оценки (Evaluation Metric)
* **Действие:** Найди раздел "Evaluation".
* **Результат:**
    * Чётко укажи, по какой **конкретной метрике** оцениваются решения (например, `ROC AUC`, `F1-score`, `RMSE`). Это критически важная информация для всего проекта.
    * Если возможно, кратко и просто объясни, что эта метрика означает в контексте данной задачи.

#### ## 5. Структура Проекта (Project Structure)
* **Действие:** Этот раздел ты создаёшь сам. Предложи **декомпозированную структуру**, которая чётко разделяет исследовательскую часть (ноутбуки) и готовую к деплою часть (сервис FastAPI). Это стандарт индустрии для проектов, которые идут в продакшн.
* **Результат:** Создай текстовое представление дерева каталогов. Оно должно быть удобным как для экспериментов, так и для последующей упаковки сервиса в Docker. Пример:
├── data/              # Неизменяемые исходные данные соревнования
├── notebooks/         # Jupyter ноутбуки для EDA 
├── src/               # Исходный код для FastAPI сервиса, скриптов предобратки данных, обучения моделей и тд (то, что пойдет в Docker)
├── artifacts/         # Обученные модели, скейлеры и другие артефакты
├── submissions/       # Сгенерированные файлы для отправки на Kaggle
├── Dockerfile         # Инструкция по сборке Docker-образа для сервиса
├── requirements.txt   # Все зависимости проекта (для EDA и сервиса)
└── README.md          # Этот файл


#### ## 6. Финальные Требования к Форматированию
* **Действие:** Убедись, что твой итоговый ответ — это **единый, готовый к использованию файл `README.md`**.
* **Результат:**
  * Весь текст должен быть отформатирован с использованием синтаксиса **Markdown**.
  * Все заголовки (от `## 1` до `## 8`) должны присутствовать и быть оформлены корректно.
  * Используй блоки кода (```) для структур папок и команд.
  * Итоговый документ должен быть профессиональным, структурированным и понятным.
